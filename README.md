# Stephen Wiendoh Portfolio

A personal portfolio landing page built with Node.js and Express. The page introduces Stephen, lists project categories, and includes a contact call to action.

## Features

- Responsive portfolio landing page
- Hero, about, projects, contact, and footer sections
- Project cards grouped by completed, in-progress, and current work
- Scroll reveal animation for project and about content
- Simple Express server setup

## Tech Stack

- Node.js
- Express
- HTML
- CSS
- JavaScript

## Project Structure

```text
.
├── package.json
├── package-lock.json
├── README.md
└── server.js
```

## Getting Started

Install the dependencies:

```bash
npm install
```

Start the server:

```bash
npm start
```

Open the site in your browser:

```text
http://localhost:3000
```

The app uses port `3000` by default. You can also set a custom port with the `PORT` environment variable.

## Customization

- Update the page content in `server.js`.
- Replace `your-email@example.com` with your real email address.
- Edit the project cards to match your latest work.
- Adjust the colors, spacing, and animations inside the `<style>` block.

## Scripts

```bash
npm start
```

Runs the Express server with `node server.js`.

## License

This project is for personal portfolio use.
